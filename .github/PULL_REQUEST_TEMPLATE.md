## What does this PR do?

<!-- One or two sentences. Link the issue if there is one. -->

## How was it tested?

<!-- e.g. `npm test` passes; deployed to a dev Worker and ran `npm run test:smoke`; manual check in Claude.ai -->

## Checklist

- [ ] `npm test` passes (typecheck + unit tests)
- [ ] No secrets committed (`wrangler.jsonc`, `.dev.vars`, `.env*` stay out of git)
- [ ] One focused change (see CONTRIBUTING.md)
- [ ] CHANGELOG.md updated if behavior changed
